# ProjectZeo — Python dependencies
# ─────────────────────────────────────────────────────────────────────────────
# BUG-1 FIX: ollama 0.3.0 had a different API (.list() returned dict, not object
# with .models).  The codebase uses the modern >=0.4.x API throughout:
#   _existing_models = {m.model for m in _ollama.Client().list().models}
# Pinning to >=0.4.0 ensures the correct response object shape.
ollama>=0.4.0

# Core runtime
annotated-types==0.6.0
anyio==3.7.1
psutil>=5.9.0
certifi==2023.7.22
charset-normalizer==3.3.2
colorama==0.4.6
contourpy==1.2.0
cycler==0.12.1
distro==1.8.0
EasyProcess==1.1
entrypoint2==1.1
exceptiongroup==1.1.3
fonttools==4.44.0
h11==0.14.0
httpcore==1.0.2
httpx>=0.25.2
idna==3.4
importlib-resources==6.1.1
kiwisolver==1.4.5
matplotlib==3.8.1
MouseInfo==0.1.3
mss==9.0.1
numpy==1.26.1
openai==1.2.3
packaging==23.2
Pillow==10.1.0
prompt-toolkit==3.0.39
PyAutoGUI==0.9.54
pydantic==2.4.2
pydantic_core==2.10.1
PyGetWindow==0.0.9
PyMsgBox==1.0.9
pyparsing==3.1.1
pyperclip==1.8.2
PyRect==0.2.0
PyScreeze==0.1.29
pytweening==1.0.7
requests==2.31.0
six==1.16.0
sniffio==1.3.0
tqdm==4.66.1
typing_extensions==4.8.0
urllib3==2.0.7
wcwidth==0.2.9
zipp==3.17.0

# AI / ML
google-generativeai==0.3.0
aiohttp==3.9.1
ultralytics==8.0.227
easyocr==1.7.1
anthropic
pyyaml==6.0

# ─────────────────────────────────────────────────────────────────────────────
# Platform-specific dependencies
# ─────────────────────────────────────────────────────────────────────────────

# Linux-only: X11 bindings for xlib-based operations
# pyscreenshot and python3-xlib are Linux/X11 specific.
# On Wayland-only systems these may not be needed; install as optional.
# pyscreenshot==3.1      # Linux/X11 only — comment out if using mss exclusively
# python3-xlib==0.15    # Linux/X11 only — comment out on pure Wayland

# BUG-12 FIX: Windows support requires pywin32 (provides win32gui, win32con).
# Not in the original requirements.txt, causing ImportError on all Windows paths.
# Install conditionally:
#   On Windows:  pip install pywin32
#   On Linux:    (skip — not available/needed)
# Uncomment the line below if targeting Windows:
# pywin32>=306   # Windows only

# macOS-only: rubicon-objc is used for macOS-specific Objective-C bridge calls.
# On Linux/Windows this package is not needed.
# rubicon-objc==0.4.7   # macOS only — uncomment if running on macOS

# ─────────────────────────────────────────────────────────────────────────────
# Optional / system tools (install via apt/brew, not pip):
# ─────────────────────────────────────────────────────────────────────────────
# sudo apt-get install xdotool wmctrl   # required on Linux/X11
# sudo apt-get install xvfb             # required for headless mode
# ollama                                # from https://ollama.com
